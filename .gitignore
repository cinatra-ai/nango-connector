# Dependencies
node_modules/

# Build output (tsconfig outDir)
dist/

# Test coverage
coverage/

# Lockfiles — extracted extension repos ship none; the cinatra monorepo owns resolution
pnpm-lock.yaml
package-lock.json
yarn.lock

# Secrets — never commit
.env
.env.*

# Logs
*.log

# OS
.DS_Store

# Internal planning notes (local-only by owner policy)
.planning/
